package com.jfk.userregistration.validators.validator;

import com.jfk.userregistration.dto.UserCreateRequestDto;
import com.jfk.userregistration.validators.result.ValidationResult;

//public class PhoneNumberValidator implements Validator {
    //@Override
   // public ValidationResult validate(Object dto) {
   //     UserCreateRequestDto userDto = (UserCreateRequestDto) dto;

       // if ((!userDto.getPhoneNumber().startsWith(String.valueOf(Enum.beeline)) ||
         //       (!userDto.getPhoneNumber().startsWith(String.valueOf(Enum.mtc)))))
        //stex  lengh-y grel naev u returny poxel teqsty
       // {
        //    return new ValidationResult(false, "Your passwords not match");


       // }
      //  System.out.println("Password validation successfully ends");
       // return success;
   // }

   //enum Enum{
         //beeline("093"), mtc("094");

        //Enum(String s) {

       // }
//    }
//}
